# 接入任务看板：每天自动推送一条成语

> 前置：已安装「任务看板」插件（dsh-task-board，侧边栏「任务看板」）。Host 需要保持运行；每次定时执行都会消耗与普通 DSH 会话相同的 **API 额度**。错过的时间点**不补跑**；同一任务正在运行时不会并发。

## 一、看板卡片（可直接粘贴）

在「任务看板」页点 **新建任务**，按下表填写，并在表单中启用「定时执行」：

| 字段 | 值 |
| --- | --- |
| 标题 | 每日成语推送 |
| 描述 | 每天 08:00 自动生成当天成语并追加到 journal.md，同时在本会话中展示。 |
| Cron | `0 8 * * *`（每天 08:00；按 Host 本地时区） |
| 权限（钉住） | `workspace-write`（允许写入 journal.md） |

**Prompt（复制到「任务指令」文本框）：**

```
你是「每日成语推送」的定时任务。启动后请完成：

1. 运行命令读取今天(按当天日期)的成语：
   node "C:\Users\ty00545\Desktop\chengyu-calendar\idiom-daily.mjs" --json
   从输出中取出：成语、拼音、四字逐字拼音、中文释义、英文释义、例句、例句拼音、例句英译。

2. 打开/创建文件 C:\Users\ty00545\Desktop\chengyu-calendar\journal.md，
   追加如下一段（用真实日期与成语替换示例）：
   ## 2026-09-09 成语「别出心裁」
   - 拼音：bié chū xīn cái
   - 释义：另想出一种与众不同的新主意。（To hit on something novel; to be original.）
   - 例句：这幅海报别出心裁，很吸引人。
      Zhou fú hǎibào bié chū xīn cái，hěn xīyǐnrén.
      The poster is original and eye-catching.

3. 在最终回复中把今天的成语完整展示出来（成语、拼音、释义、例句）。

请用 todo_write 维护步骤，最后一条标记为 completed；没有剩余工作时不要保留 in_progress。
```

保存后，卡片处于「待执行」列出；到点后 Host 会创建一次独立会话执行上面的指令，并把执行结果结算到该卡片。

## 二、用命令行先验证一次

无需等到点，当场看今天会是哪一条：

```powershell
node "C:\Users\ty00545\Desktop\chengyu-calendar\idiom-daily.mjs"
node "C:\Users\ty00545\Desktop\chengyu-calendar\idiom-daily.mjs" --json
```

## 三、说明与限制

- **每次运行都消耗 API 额度**；如果只想本地生成、不跑 Agent，直接运行上面的 `idiom-daily.mjs`（不消耗 Agent 额度）。
- Host 停止、系统睡眠或长暂停期间错过的触发点会**跳过，绝不排队补跑**。
- 已启用计划会从未来触发点前持续持锁，可能略微增加耗电——不需要时可在卡片里把「定时执行」关掉。
- Cron 以 **Host 本地时区**为基准；卡片的「权限」钉 `workspace-write` 才能写 journal.md。
- 想改时间或换个成语来源，改 Prompt 里的命令即可；成语库在 `data.js / data2.js / data3.js`。
